/*global describe, beforeEach, it, inject, expect, spyOn*/

describe('Spinner Directive', function() {
/*

  var element, scope;

  beforeEach(module('/templates/spinner.html'));
  beforeEach(module('app.directives'));

  beforeEach(inject(function($compile, $rootScope) {
    scope = $rootScope.$new();
    scope.data = null;
    element = angular.element('<spinner data="data"></spinner>');
    $compile(element)(scope);
    scope.$digest();
  }));

  it('should have a child element with spinner class', function() {
    expect(element.children()).toHaveClass('spinner');
  });

  it('should load remove the element from DOM when data is available in the scope', function() {
    spyOn(element, 'remove');
    scope.$apply(function() {
      scope.data = true;
    });
    expect(element.remove).toHaveBeenCalled();
  });
*/

});
